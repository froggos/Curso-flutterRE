import 'package:flutter/material.dart';

class Preguntas extends StatefulWidget {
  const Preguntas({super.key});

  @override
  State<Preguntas> createState() {
    return _Preguntas();
  }
}

class _Preguntas extends State<Preguntas> {
  @override
  Widget build(BuildContext context) {
    return const Text("Pantalla de preguntas");
  }
}
